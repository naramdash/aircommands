# Agent Instructions

- Do not start the local dev server automatically to inspect or verify behavior.
- Prefer static inspection, targeted tests, and production builds for verification.
- Prefer Bun commands over npm commands for package scripts and dependency management.
- If runtime verification through the dev server is genuinely needed, ask the user first and wait for explicit approval.
- Do not open browsers or launch GUI apps unless the user explicitly asks for it.
